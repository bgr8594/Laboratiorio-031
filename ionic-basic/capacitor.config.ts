import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ionic-basic-2034228',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
