import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.democapacitor.app',
  appName: 'ionic-basic-1942487',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
